{
  "description": "{{DESCRIPTION}}",
  "hooks": {
    "{{EVENT}}": [
      {
        "matcher": "{{MATCHER}}",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/{{SCRIPT_NAME}}.js",
            "timeout": {{TIMEOUT}}
          }
        ]
      }
    ]
  }
}
